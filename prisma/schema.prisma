// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider    = "prisma-client"
  output      = "../app/generated/prisma"
  engineType  = "client"
}

datasource db {
  provider = "sqlite"
}

<<<<<<< HEAD
model Intern {
  id String @id @default(uuid()) @map("ID")
=======
//USER entity is both site manager and intern
model User {
  ID String @id @default(uuid()) @map("ID") //uniquely identifiable ID mapped to ID
>>>>>>> f887956737c57187e148e1b84f39357fb1540824

  //user contact information
  user_name  String
  user_email String //@unique
  user_phone String //@unique

<<<<<<< HEAD
  next_shift_ID String?
  next_shift    Scheduled_day? @relation("next_shift", fields: [next_shift_ID], references: [id])

  site_manager Site_Manager? @relation("site_manager_profile")
=======
  //employee status
  intern       Intern?
  site_manager Site_Manager?
>>>>>>> f887956737c57187e148e1b84f39357fb1540824

  //collection of their attendance history and future scheduled shifts
  attendance_history Attendance[]

<<<<<<< HEAD
  created_shift_requests   Shift_Request[] @relation("created_shift_requests")
  picked_up_shift_requests Shift_Request[] @relation("picked_up_shift_requests")
=======
  //where the employee works and or manages
  //---------------------------------------------------------
  works_At String
  location Location @relation("interns", fields: [works_At], references: [location_ID])
}

//extension of user roles for interns 
model Intern {
  Intern_ID String @id @map("ID")
  User      User   @relation(fields: [Intern_ID], references: [ID], onDelete: Cascade, onUpdate: Cascade)
>>>>>>> f887956737c57187e148e1b84f39357fb1540824
}

//extension of user roles for Site managers also contains their managed site
model Site_Manager {
  id String @id @default(uuid())

  intern_ID String @unique
  intern    Intern @relation("site_manager_profile", fields: [intern_ID], references: [id])

<<<<<<< HEAD
  locations Location[]
}

model Location {
  id String @id @default(uuid())

  location_name String
  address       String

  attendance_code String?
  code_expires_at DateTime?

  managers       Site_Manager[]
  scheduled_days Scheduled_day[]
}

model Scheduled_day {
  id String @id @default(uuid())

  start_time DateTime
  end_time   DateTime

  site_ID  String
  location Location @relation(fields: [site_ID], references: [id])

  attendance Attendance[]

  next_shift_for Intern[] @relation("next_shift")
=======
  //-----------------------------------------
  managed_site Location? @relation("manager", fields: [manager_ID], references: [location_ID])
}

model Location {
  location_ID    String   @id @default(uuid()) //location identifier
  //location information name contact days used and times
  location_name  String   //@unique
  address        String   
  operation_days Work_day
  open_time      Int
  close_time     Int

  //generated code to check attendance at a location during a workday
  attendance_code String

  //list of interns and site manager of specific location
  //userId  String        @unique
  interns User[]        @relation("interns")
  manager Site_Manager? @relation("manager")

  site     Scheduled_day[] 
>>>>>>> f887956737c57187e148e1b84f39357fb1540824
}

//1 to many user has many attendances,
model Attendance {
  id String @id @default(uuid()) //unique identifier

  //clock in time and out time and current attendance status
  clock_in_time  DateTime?
  clock_out_time DateTime?
  status         Attendance_status @default(ABSENT) //defualt being absent

<<<<<<< HEAD
  internID String
  intern   Intern @relation(fields: [internID], references: [id])

  shift_ID      String
  scheduled_day Scheduled_day @relation(fields: [shift_ID], references: [id])

  shift_request_ID String?
  shift_request    Shift_Request? @relation(fields: [shift_request_ID], references: [id])
}

model Shift_Request {
  id String @id @default(uuid())

  reason    String?
  createdAt DateTime @default(now())

  created_by_ID String
  created_by    Intern @relation("created_shift_requests", fields: [created_by_ID], references: [id])

  picked_up_by_ID String?
  picked_up_by    Intern? @relation("picked_up_shift_requests", fields: [picked_up_by_ID], references: [id])

  attendances Attendance[]
}

model Admin {
  id String @id @default(uuid())

  name  String
  email String
=======
  //absence requests including the status, reason(optional), and covering intern(optional)
  request_status Leave_request? @default(PENDING)
  reason         String?

  //id and relationship to the user
  userID String
  User   User   @relation(fields: [userID], references: [ID])
}

model Scheduled_day {
  // shift identifier
  shift_ID String @id @default(uuid())

  // shift start time (date + time)
  date DateTime

  // duration of shift in minutes
  shift_duration Int

  // location where shift occurs
  site_ID String
  site Location @relation(fields: [site_ID], references: [location_ID])

  // shift type
  status Shift_type @default(on_site)

  // relationship with user
  userID String
  User   User @relation(fields: [userID], references: [ID])
>>>>>>> f887956737c57187e148e1b84f39357fb1540824
}

//the below are enums that are used to signify status's
enum Attendance_status {
<<<<<<< HEAD
  ABSENT
  PRESENT
  CALLED_OFF
  COVERING_SHIFT
}
=======
  ABSENT //If the intern is absent
  PRESENT //if the intern is actively present
  CALLED_OFF //if the intern had called off in advance   
  //EXCUSED           //excused abscence doctors note etc
  COVERING_SHIFT //for a intern covering a shift of another intern
}

enum Leave_request {
  PENDING
  ACCEPTED
  REJECTED
}

enum Shift_type {
  remote
  office_hours
  on_site
}

enum Work_day {
  MONDAY
  TUESDAY
  WEDNESDAY
  THURSDAY
  FRIDAY
  SATURDAY
  SUNDAY
}
>>>>>>> f887956737c57187e148e1b84f39357fb1540824
